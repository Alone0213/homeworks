# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Bash(mkdir -p:*)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && flake8 app.py 2>/dev/null; python -c \"import flask; print('Flask OK:', flask.__version__)\")", "Bash(pip install flask 2>&1 | tail -3)", "Bash(python -m pip install flask --timeout 30 2>&1 | tail -5)", "Bash(pip install flask -i https://pypi.tuna.tsinghua.edu.cn/simple --timeout 60 2>&1 | tail -5)", "Bash(pip install flask -i https://pypi.tuna.tsinghua.edu.cn/simple --timeout 120 2>&1 | tail -5)", "Bash(python -c \"import flask; print('Flask OK:', flask.__version__)\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && timeout 5 python app.py 2>&1 || true)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && ls -la *.sql *.txt *.py *.pptx templates/)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nimport sqlite3\nconn = sqlite3.connect('furniture.db')\nconn.row_factory = sqlite3.Row\ntables = conn.execute(\\\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\\\").fetchall()\nprint('=== 数据库表 ===')\nfor t in tables:\n    cnt = conn.execute(f'SELECT COUNT(*) FROM [{t[\\\"name\\\"]}]').fetchone()[0]\n    print(f'  {t[\\\"name\\\"]}: {cnt} 条记录')\nconn.close()\n\")", "Bash(rm \"C:\\Users\\du200\\Documents\\ds\\家具城进销存管理系统_数据库设计.pptx\":*)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nimport sqlite3\nconn = sqlite3.connect('furniture.db')\nconn.row_factory = sqlite3.Row\ntables = conn.execute(\\\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\\\").fetchall()\nfor t in tables:\n    cnt = conn.execute(f'SELECT COUNT(*) FROM [{t[\\\"name\\\"]}]').fetchone()[0]\n    print(f'{t[\\\"name\\\"]}: {cnt} records')\nconn.close()\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nwith open('create_ppt.py', 'r', encoding='utf-8') as f:\n    for i, line in enumerate(f, 1):\n        if '\\u201c' in line or '\\u201d' in line:\n            print(f'Line {i}: {line.rstrip()[:100]}')\n\")", "Bash(rm \"C:\\Users\\du200\\Documents\\ds\\家具城进销存管理系统_技术实现详解.pptx\":*)", "Bash(python -c \"import app; print('app.py imports OK')\" 2>&1 || echo \"NOT_OK\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nfrom pptx import Presentation\nprs = Presentation('家具城进销存管理系统_SQL实现分析.pptx')\nprint(f'Slides: {len(prs.slides)}')\nfor i, sl in enumerate(prs.slides):\n    shapes = len(sl.shapes)\n    texts = []\n    for s in sl.shapes:\n        if s.has_text_frame:\n            txt = s.text_frame.text[:40].replace('\\n',' ')\n            texts.append(txt)\n    print(f'  Slide {i+1}: {shapes} shapes | {texts[0] if texts else \\\"empty\\\"}')\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nfrom pptx import Presentation\nprs = Presentation('家具城进销存管理系统_SQL实现分析.pptx')\nfor i, sl in enumerate(prs.slides):\n    print(f'=== Slide {i+1} ===')\n    for s in sl.shapes:\n        if s.has_text_frame:\n            txt = s.text_frame.text[:60].replace(chr(10),' | ')\n            print(f'  [{s.shape_type}] {txt}')\n    print()\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && ls *.pptx)", "Bash(pip show python-docx 2>/dev/null || pip install python-docx -i https://pypi.tuna.tsinghua.edu.cn/simple 2>&1 | tail -3)", "Bash(pip install python-docx -i https://pypi.tuna.tsinghua.edu.cn/simple 2>&1 | tail -5)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python create_doc.py)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && rm -f \"家具城进销存管理系统_SQL实现分析.pptx\" \"家具城进销存管理系统_技术实现详解.pptx\" create_ppt.py 2>/dev/null; ls *.docx *.sql *.py *.txt templates/ 2>/dev/null)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nfrom docx import Document\ndoc = Document('家具城进销存管理系统_SQL实现文档.docx')\nprint(f'Total paragraphs: {len(doc.paragraphs)}')\nprint(f'Total tables: {len(doc.tables)}')\nprint()\n# Print headings\nfor p in doc.paragraphs:\n    if p.style.name.startswith('Heading'):\n        print(f'[{p.style.name}] {p.text}')\nprint()\n# Print table info\nfor i, t in enumerate(doc.tables):\n    cols = len(t.columns)\n    rows = len(t.rows)\n    hdr = ' | '.join([c.text[:15] for c in t.rows[0].cells])\n    print(f'Table {i+1}: {rows}x{cols} | {hdr}')\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nfrom docx import Document\ndoc = Document('家具城进销存管理系统_SQL实现文档.docx')\nprint(f'段落数: {len(doc.paragraphs)}')\nprint(f'表格数: {len(doc.tables)}')\nprint(f'一级标题: {sum(1 for p in doc.paragraphs if p.style.name == \\\"Heading 1\\\")}')\nprint(f'二级标题: {sum(1 for p in doc.paragraphs if p.style.name == \\\"Heading 2\\\")}')\n# Check code blocks look right\nfrom docx.oxml.ns import qn\ncode_count = 0\nfor p in doc.paragraphs:\n    for r in p.runs:\n        if r.font.name == 'Consolas' and len(r.text) > 10:\n            code_count += 1\n            break\nprint(f'代码块数: {code_count}')\nprint()\nprint('文档大小: 45KB')\nprint('状态: OK')\n\")", "Bash(rm -f \"C:\\Users\\du200\\Documents\\ds\\家具城进销存管理系统_SQL实现文档.docx\" && cd \"C:\\Users\\du200\\Documents\\ds\" && python create_doc.py)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && mv FurnitureDB_SQL_Doc.docx \"家具城进销存管理系统_SQL实现文档.docx\" 2>/dev/null || python -c \"import os; os.rename('FurnitureDB_SQL_Doc.docx', '家具城进销存管理系统_SQL实现文档.docx')\" && ls -la *.docx)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && ls -la *.docx)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && rm -f \"~$城进销存管理系统_SQL实现文档.docx\" 2>/dev/null; python -c \"\nimport os, time\n# Wait then try to replace\nold = '家具城进销存管理系统_SQL实现文档.docx'\nnew = 'FurnitureDB_SQL_Doc.docx'\nif os.path.exists(old):\n    os.remove(old)\n    time.sleep(0.5)\nos.rename(new, old)\nprint('Renamed OK')\n\" && ls -la *.docx)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nfrom docx import Document\ndoc = Document('FurnitureDB_SQL_Doc.docx')\nprint(f'段落数: {len(doc.paragraphs)}')\nprint(f'表格数: {len(doc.tables)}')\nfor p in doc.paragraphs:\n    if p.style.name.startswith('Heading'):\n        print(f'  [{p.style.name}] {p.text}')\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nimport sqlite3, importlib.util\nspec = importlib.util.spec_from_file_location('app', 'app.py')\n# Just verify the file imports clean\nimport ast\nwith open('app.py', encoding='utf-8') as f:\n    tree = ast.parse(f.read())\nroutes = [n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and any(\n    d.attr == 'route' for d in ast.walk(n) if isinstance(d, ast.Attribute)\n)]\nprint(f'路由函数数: {len(routes)}')\nfor r in routes:\n    print(f'  def {r.name}')\n\")", "Bash(python -c \"from docx import Document; doc=Document('FurnitureDB_SQL_Doc.docx'); print('段落:', len(doc.paragraphs), '表格:', len(doc.tables))\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && rm -f \"FurnitureDB_SQL_Doc.docx\" && python create_doc.py 2>&1)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nfrom docx import Document\ndoc = Document('FurnitureDB_SQL_Doc.docx')\nprint(f'段落: {len(doc.paragraphs)}, 表格: {len(doc.tables)}')\nfor p in doc.paragraphs:\n    if p.style.name.startswith('Heading'):\n        print(f'  [{p.style.name}] {p.text}')\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && rm -f \"家具城进销存管理系统_SQL实现文档.docx\" 2>/dev/null; cp FurnitureDB_SQL_Doc.docx \"家具城进销存管理系统_SQL实现文档.docx\" 2>/dev/null; ls -la *.docx)", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nimport sqlite3\nconn = sqlite3.connect('furniture.db')\nconn.row_factory = sqlite3.Row\n\n# 销售总收入\nincome = conn.execute('SELECT COALESCE(SUM(Quantity * UnitPrice), 0) FROM Sales').fetchone()[0]\n# 入库总成本\nexpense = conn.execute('SELECT COALESCE(SUM(Quantity * UnitPrice), 0) FROM WarehouseEntry').fetchone()[0]\n# 实收现金\nreceipt = conn.execute('SELECT COALESCE(SUM(Amount), 0) FROM Receipt').fetchone()[0]\n# 销售笔数\nscnt = conn.execute('SELECT COUNT(*) FROM Sales').fetchone()[0]\n# 入库笔数\necnt = conn.execute('SELECT COUNT(*) FROM WarehouseEntry').fetchone()[0]\n\nconn.close()\nprint(f'销售总收入: ¥{income:.2f} ({scnt}笔)')\nprint(f'入库总成本: ¥{expense:.2f} ({ecnt}笔)')\nprint(f'毛利润:     ¥{income - expense:.2f}')\nprint(f'实收现金:   ¥{receipt:.2f}')\n\")", "Bash(cd \"C:\\Users\\du200\\Documents\\ds\" && python -c \"\nimport sqlite3\nconn = sqlite3.connect('furniture.db')\nincome = conn.execute('SELECT COALESCE(SUM(Quantity * UnitPrice), 0) FROM Sales').fetchone()[0]\nexpense = conn.execute('SELECT COALESCE(SUM(Quantity * UnitPrice), 0) FROM WarehouseEntry').fetchone()[0]\nreceipt = conn.execute('SELECT COALESCE(SUM(Amount), 0) FROM Receipt').fetchone()[0]\nscnt = conn.execute('SELECT COUNT(*) FROM Sales').fetchone()[0]\necnt = conn.execute('SELECT COUNT(*) FROM WarehouseEntry').fetchone()[0]\nconn.close()\nprint(f'Income:{income:.2f} Sales:{scnt}  Expense:{expense:.2f} Entry:{ecnt}  Profit:{income-expense:.2f}  Receipt:{receipt:.2f}')\n\")", "Bash(python -c \"import graphviz; print(graphviz.__version__)\" 2>&1 || echo \"NOT_INSTALLED\")", "Bash(python -c \"import graphviz; print(graphviz.__version__)\")", "Bash(pip install graphviz 2>&1 | tail -5)", "Bash(where dot 2>$null; if ($?) { dot -V 2>&1 } else { Write-Output \"DOT_NOT_FOUND\" })", "Bash(pip install graphviz 2>&1 | Select-Object -Last 5)", "Bash(Get-Command dot -ErrorAction SilentlyContinue; if ($?) { & dot -V 2>&1 } else { Write-Output \"DOT_NOT_FOUND\" })", "Bash(pip show graphviz 2>&1 | Select-String \"Location\")", "Bash(.venv\\Scripts\\python -c \"\nprint('=' * 72)\nprint('  家具城进销存管理系统 — 数据库架构分析报告')\nprint('  数据库名称: FurnitureDB')\nprint('=' * 72)\n\")", "Edit(analyze_schema.py)", "Bash(.venv\\Scripts\\python analyze_schema.py:*)", "Bash(Remove-Item analyze_schema.py:*)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
